Options -Indexes
    RewriteEngine on
    IndexIgnore */*

    RewriteBase /NAZOV_PROJEKTU/

    RewriteRule ^admin/?(.*)$ backend/web/$1 [QSA,L]

    RewriteCond %{REQUEST_URI} !^/frontend/web/
    RewriteRule ^(.*)$ frontend/web/$1 [QSA,L]